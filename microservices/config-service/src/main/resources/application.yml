server:
  port: 8888

spring:
  application:
    name: config-service
  datasource:
    url: jdbc:h2:mem:opencms_config
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

logging:
  level:
    org.opencms.config: DEBUG
    org.springframework.web: INFO
